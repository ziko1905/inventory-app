<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="../../css/styles.css">
</head>
<body>
    <%- include("./partials/navbar.ejs") %>
    <h1><%= title %></h1>
    <div class="info">
        <h2>Product info:</h2>
        <%- include("./partials/infoTable", {info: category}) %>
    </div>
    <div class="buttons-div">
        <a class="update-btn" href="/categories/<%= category.id %>/update">Update</a>
        <form action="/categories/<%= category.id %>/delete" method="POST"> 
            <button class="delete-btn" type="submit" onclick="confirm('Are you sure you want to delete this category?')">Delete</button>
        </form>
    </div>
</body>
</html>